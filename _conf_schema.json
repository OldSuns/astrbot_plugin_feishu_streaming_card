{
  "enabled": {
    "title": "启用流式卡片",
    "type": "bool",
    "default": true,
    "description": "是否启用飞书流式卡片增强功能"
  },
  "show_thinking": {
    "title": "显示思考过程",
    "type": "bool",
    "default": true,
    "description": "是否在卡片中显示 LLM 的思考过程"
  },
  "show_tools": {
    "title": "显示工具调用",
    "type": "bool",
    "default": true,
    "description": "是否在卡片中显示工具调用历史"
  },
  "tool_limit": {
    "title": "工具调用显示数量",
    "type": "int",
    "default": 5,
    "minimum": 0,
    "maximum": 50,
    "description": "最多显示几个工具调用，超出显示+n。"
  },
  "tool_range": {
    "title": "工具调用显示范围",
    "type": "string",
    "default": "latest",
    "enum": [
      "latest",
      "earliest"
    ],
    "description": "工具调用显示范围 latest:最新，earliest:最早"
  },
  "show_footer": {
    "title": "显示统计信息",
    "type": "bool",
    "default": true,
    "description": "是否显示耗时、模型、token 等统计信息"
  },
  "footer_style": {
    "title": "统计信息样式",
    "type": "string",
    "default": "small",
    "enum": [
      "small",
      "md"
    ],
    "description": "统计信息样式 small:小字，md:灰字Markdown"
  },
  "max_sessions": {
    "title": "最大会话数",
    "type": "int",
    "default": 25,
    "minimum": 10,
    "maximum": 1000,
    "description": "内存中保持的最大会话数，超过后自动清理最旧的会话"
  },
  "session_ttl": {
    "title": "会话超时时间（秒）",
    "type": "int",
    "default": 3600,
    "minimum": 300,
    "maximum": 86400,
    "description": "会话在内存中保持的最长时间，超时后自动清理"
  },
  "uninstall_patch": {
    "title": "卸载 Patch",
    "type": "bool",
    "default": false,
    "description": "卸载 Monkey Patch"
  },
  "debug_mode": {
    "title": "调试模式",
    "type": "bool",
    "default": false,
    "description": "开启后会输出详细的调试日志"
  }
}
